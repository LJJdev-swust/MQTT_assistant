-- Migration 002（示例，预留给将来的表结构变更）
-- 版本: v1 -> v2
-- 说明: 此文件为预留示例，实际结构变更请复制此文件并命名为 migration_00X.sql
--       同时在 DatabaseManager::applyMigrations() 中添加对应的迁移逻辑。
--
-- 建议命名格式：migration_NNN_描述.sql
-- 每次新增表字段或修改表结构时，递增版本号并记录变更。

SELECT 1; -- 未来迁移内容写在此处
